<% if current_user.is_friend_of?(@user.id) %>
  <%= link_to "unfollow", friends_path(current_user.friends.find_by(follower_id: @user.id)), method: :delete %>
<% else %>
  <%= link_to "follow", friends_path(current_user.friends.build, friend_id: @user.id), method: :post %>
<% end %>
